import { Router } from 'express';
// import { uploadFile, upload } from '../controllers/filesController';

const router = Router();

// router.post('/upload', upload.single('file'), uploadFile);

export default router;
